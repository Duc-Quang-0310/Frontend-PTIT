import { FC } from 'react';

interface ProductUserCommentProps {
  id?: string;
}

const ProductUserComment: FC<ProductUserCommentProps> = ({ id }) => {
  return <div>ProductUserComment</div>;
};

export default ProductUserComment;
